# Thessaloniki interactive map — Python dependencies
#
# Conda (recommended, GDAL/GEOS/PROJ binaries come prebuilt):
#   conda create -n interactive-map -c conda-forge python=3.12 --file requirements.txt
#
# pip (inside an activated env):
#   pip install -r requirements.txt

# --- Geospatial core ---
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
pyogrio>=0.9          # fast GeoPackage I/O engine used by GeoPandas
fiona>=1.10           # fallback OGR engine

# --- Data handling ---
pandas>=2.2
numpy>=1.26

# --- Web mapping ---
folium>=0.17
branca>=0.7           # MacroElement / Template used by the zoom-control classes

# --- Classification helpers (choropleth binning) ---
# NaturalBreaks is the Jenks classifier behind the density choropleth,
# and matches what QGIS produces for the same column.
mapclassify>=2.6

# --- Pedestrian-network analysis ---
# Shortest paths over the noded walking network: bus-metro integration
# and the station walking catchments. See src/thessmap/walknet.py.
networkx>=3.0

# --- Population rasters ---
# Reprojecting the 100 m GHSL grid to Web Mercator and colouring it to a
# PNG, since MapLibre reads no GeoTIFF. Pillow writes the PNG.
rasterio>=1.3
pillow>=10.0

# --- Notebook environment ---
jupyterlab>=4.2
ipykernel>=6.29
